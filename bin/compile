#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

set -exuo pipefail

BUILD_DIR=${1:-}
CACHE_DIR=${2:-}
ENV_DIR=${3:-}

cd $BUILD_DIR

cp ai/service/text2img/requirements.txt .
cp data/migrate_posts/package.json .
cp data/migrate_posts/yarn.lock .

exit 0
